diff --git a/api/module-lib-current.txt b/api/module-lib-current.txt
index 15741a7805..6850dab2b1 100644
--- a/api/module-lib-current.txt
+++ b/api/module-lib-current.txt
@@ -44,6 +44,7 @@ package android.system {
     method public static int getsockoptInt(@NonNull java.io.FileDescriptor, int, int) throws android.system.ErrnoException;
     method @Nullable public static android.system.StructLinger getsockoptLinger(@NonNull java.io.FileDescriptor, int, int) throws android.system.ErrnoException;
     method public static int ioctlInt(@NonNull java.io.FileDescriptor, int) throws android.system.ErrnoException;
+    method public static int is_in_esm_wait(int) throws android.system.ErrnoException;
     method @Nullable public static java.io.FileDescriptor[] pipe2(int) throws android.system.ErrnoException;
     method @Nullable public static String realpath(@Nullable String) throws android.system.ErrnoException;
     method public static void setpgid(int, int) throws android.system.ErrnoException;
diff --git a/luni/src/main/java/android/system/Os.java b/luni/src/main/java/android/system/Os.java
index 9beb87864a..686b0c5d81 100755
--- a/luni/src/main/java/android/system/Os.java
+++ b/luni/src/main/java/android/system/Os.java
@@ -363,6 +363,18 @@ public final class Os {
      */
     public static int gettid() { return Libcore.os.gettid(); }
 
+    /**
+     * Check if a thread is currently blocked in esm_wait().
+     * Used by watchdog to distinguish ESM wait from deadlocks.
+     *
+     * @param tid Thread ID to check
+     * @return 1 if thread is in TASK_EV_WAIT state, 0 otherwise
+     * @hide
+     */
+    @SystemApi(client = MODULE_LIBRARIES)
+    @libcore.api.CorePlatformApi(status = libcore.api.CorePlatformApi.Status.STABLE)
+    public static int is_in_esm_wait(int tid) throws ErrnoException { return Libcore.os.is_in_esm_wait(tid); }
+
     /**
      * See <a href="http://man7.org/linux/man-pages/man2/getuid.2.html">getuid(2)</a>.
      */
diff --git a/luni/src/main/java/libcore/io/ForwardingOs.java b/luni/src/main/java/libcore/io/ForwardingOs.java
index 8320be0546..5972fdce73 100755
--- a/luni/src/main/java/libcore/io/ForwardingOs.java
+++ b/luni/src/main/java/libcore/io/ForwardingOs.java
@@ -384,6 +384,11 @@ public class ForwardingOs implements Os {
      */
     public int gettid() { return os.gettid(); }
 
+    /**
+     * @hide
+     */
+    public int is_in_esm_wait(int tid) throws ErrnoException { return os.is_in_esm_wait(tid); }
+
     /**
      * @hide
      */
diff --git a/luni/src/main/java/libcore/io/Linux.java b/luni/src/main/java/libcore/io/Linux.java
index 4ecb7852fe..913a00d3f0 100755
--- a/luni/src/main/java/libcore/io/Linux.java
+++ b/luni/src/main/java/libcore/io/Linux.java
@@ -104,6 +104,7 @@ public final class Linux implements Os {
     public native StructTimeval getsockoptTimeval(FileDescriptor fd, int level, int option) throws ErrnoException;
     public native StructUcred getsockoptUcred(FileDescriptor fd, int level, int option) throws ErrnoException;
     public native int gettid();
+    public native int is_in_esm_wait(int tid) throws ErrnoException;
     public native int getuid();
     public native byte[] getxattr(String path, String name) throws ErrnoException;
     public native StructIfaddrs[] getifaddrs() throws ErrnoException;
diff --git a/luni/src/main/java/libcore/io/Os.java b/luni/src/main/java/libcore/io/Os.java
index 0c58455cf5..2e01fc6048 100755
--- a/luni/src/main/java/libcore/io/Os.java
+++ b/luni/src/main/java/libcore/io/Os.java
@@ -323,6 +323,11 @@ public interface Os {
      */
     public int gettid();
 
+    /**
+     * @hide
+     */
+    public int is_in_esm_wait(int tid) throws ErrnoException;
+
     /**
      * @hide
      */
diff --git a/luni/src/main/native/libcore_io_Linux.cpp b/luni/src/main/native/libcore_io_Linux.cpp
index d7b6c22aa5..bbb0db544c 100755
--- a/luni/src/main/native/libcore_io_Linux.cpp
+++ b/luni/src/main/native/libcore_io_Linux.cpp
@@ -52,6 +52,7 @@
 
 #if defined(__BIONIC__)
 #include <android/fdsan.h>
+#include <sys/esm.h>
 #endif
 
 #include <android-base/file.h>
@@ -1594,6 +1595,21 @@ static jint Linux_gettid(JNIEnv* env __unused, jobject) {
 #endif
 }
 
+static jint Linux_is_in_esm_wait(JNIEnv* env, jobject, jint tid __unused) {
+#if defined(__BIONIC__)
+    int result = TEMP_FAILURE_RETRY(is_in_esm_wait(tid));
+    if (result == -1) {
+        throwErrnoException(env, "is_in_esm_wait");
+        return -1;
+    }
+    return result;
+#else
+    // Not available on host
+    throwErrnoException(env, "is_in_esm_wait");
+    return -1;
+#endif
+}
+
 static jint Linux_getuid(JNIEnv*, jobject) {
     return getuid();
 }
@@ -2795,6 +2811,7 @@ static JNINativeMethod gMethods[] = {
     NATIVE_METHOD(Linux, getsockoptTimeval, "(Ljava/io/FileDescriptor;II)Landroid/system/StructTimeval;"),
     NATIVE_METHOD(Linux, getsockoptUcred, "(Ljava/io/FileDescriptor;II)Landroid/system/StructUcred;"),
     NATIVE_METHOD(Linux, gettid, "()I"),
+    NATIVE_METHOD(Linux, is_in_esm_wait, "(I)I"),
     NATIVE_METHOD(Linux, getuid, "()I"),
     NATIVE_METHOD(Linux, getxattr, "(Ljava/lang/String;Ljava/lang/String;)[B"),
     NATIVE_METHOD(Linux, getifaddrs, "()[Landroid/system/StructIfaddrs;"),
